function Ship(x,y,img,id)
{
    this.x = x;
    this.y = y;
    this.img = img;
    this.id = id;
    this.dx = 0;
    thid.dy = 0.01;
}

Ship.prototypes.update = function(x,y)
{
    this.x = x;
    this.y = y;
}

function ShipIcon(ship)
{



}

ShipIcon.prototypes.
